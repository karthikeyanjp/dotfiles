# Starship Prompt - Professional Coding Setup
# Minimal, fast, informative

format = """
[┌─](bold blue)$username$hostname$directory$git_branch$git_status$nodejs$python$rust$golang$docker_context
[└─](bold blue)$character"""

# Continuation prompt (multiline commands)
continuation_prompt = "[▸▹ ](dimmed white)"

[character]
success_symbol = "[❯](bold green)"
error_symbol = "[❯](bold red)"
vimcmd_symbol = "[❮](bold green)"

[directory]
truncation_length = 4
truncate_to_repo = true
style = "bold cyan"
read_only = " 🔒"
home_symbol = "~"
truncation_symbol = "…/"

[git_branch]
symbol = " "
style = "bold purple"
format = "[$symbol$branch(:$remote_branch)]($style) "

[git_status]
style = "bold yellow"
format = "([$all_status$ahead_behind]($style) )"
conflicted = "⚔️ "
ahead = "⇡${count}"
behind = "⇣${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
untracked = "?${count}"
stashed = "📦${count}"
modified = "!${count}"
staged = "+${count}"
renamed = "»${count}"
deleted = "✘${count}"

[git_commit]
tag_symbol = " 🏷️ "
tag_disabled = false

[nodejs]
symbol = " "
style = "bold green"
format = "via [$symbol($version )]($style)"
detect_extensions = ["js", "mjs", "cjs", "ts", "tsx", "jsx"]

[python]
symbol = " "
style = "bold yellow"
format = "via [$symbol$version]($style) "
detect_extensions = ["py"]

[rust]
symbol = " "
style = "bold red"
format = "via [$symbol($version )]($style)"

[golang]
symbol = " "
style = "bold cyan"
format = "via [$symbol($version )]($style)"

[docker_context]
symbol = " "
style = "bold blue"
format = "via [$symbol$context]($style) "
only_with_files = true

[aws]
symbol = "  "
style = "bold yellow"
format = 'on [$symbol($profile )($region )]($style)'
disabled = false

[kubernetes]
symbol = "☸ "
style = "bold blue"
format = 'on [$symbol$context( $namespace)]($style) '
disabled = false

[terraform]
symbol = "💠 "
style = "bold purple"
format = "via [$symbol$version]($style) "

[package]
symbol = "📦 "
style = "bold 208"
format = "is [$symbol$version]($style) "
disabled = true

[username]
style_user = "bold yellow"
style_root = "bold red"
format = "[$user]($style)@"
disabled = false
show_always = false

[hostname]
ssh_only = true
style = "bold green"
format = "[$hostname]($style) in "
disabled = false

[time]
disabled = false
style = "bold white"
format = "at [$time]($style) "
time_format = "%T"
utc_time_offset = "local"

[cmd_duration]
min_time = 2000
style = "bold yellow"
format = "took [$duration]($style) "

[battery]
full_symbol = "🔋"
charging_symbol = "⚡"
discharging_symbol = "💀"
disabled = true

[[battery.display]]
threshold = 30
style = "bold red"
